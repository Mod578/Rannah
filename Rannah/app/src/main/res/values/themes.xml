<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Bal" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/bal_paper</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!--
          العرض من حافة إلى حافة مقصود: أندرويد ١٥ يفرضه على ما يستهدف SDK 35،
          والتطبيق يتعامل مع الحواف بـWindowInsets لا بحشو ثابت. لا يُفعَّل
          android:windowOptOutEdgeToEdgeEnforcement، فأندرويد ١٦ يتجاهله أصلًا،
          ولا يُضبَط enforce*Contrast هنا: enableEdgeToEdge() يتولّاه على واجهة
          ٢٩ فما فوق، وهو غير موجود على واجهة ٢٦ التي يدعمها التطبيق.
        -->
    </style>

    <!--
      شاشة البداية: الشعار على أرضية التطبيق نفسها. كانت أرضية حِبرية غريبة
      عن كل شاشة في التطبيق، فيراها المستخدم لحظةً ثم تُستبدل — وهو أوضح ما
      يجعل الإطلاق يبدو مركّبًا لا مصمَّمًا.
    -->
    <style name="Theme.Bal.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Bal</item>
    </style>
</resources>
